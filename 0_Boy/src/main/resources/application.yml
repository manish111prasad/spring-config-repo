eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka
management:
  endpoints:
    web:
      exposure:
        include: "*"

spring:
  application:
    name: Dick
  boot:
    admin:
      client:
        url: http://localhost:1111/
